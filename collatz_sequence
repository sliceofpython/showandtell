import random
print(n)
while True:
    print('Enter an integer between 1 and 1000. Enter 'r' for a random integer. Enter "exit" to exit.')
    n = input()
    r = random.randint(1, 1000)
    if n == 'exit' or 'Exit':
        break
    while n != 1:
        if n % 2 == 0:
            print(n // 2)
            n = n // 2
        elif n % 2 == 1:
           print((n*3) + 1)
            n = (n*3) + 1
    print('Done.')
